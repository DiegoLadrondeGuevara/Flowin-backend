# ── Genesis Custom Ignores ────────────────────────────────

# Seguridad: NUNCA subir las llaves (Stripe, Twilio, Google)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# AWS SAM: Archivos temporales de construcción
.aws-sam/
samconfig.toml

# Node.js: Dependencias (pesan mucho y se instalan en la nube)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs y Temporales
logs
*.log
.DS_Store